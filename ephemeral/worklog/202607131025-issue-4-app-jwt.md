# Issue 4: Delegate GitHub App JWT authentication

started: 2026-07-13 10:25 CST
goal: Solve GitHub issue #4 in an isolated worktree, preserving existing PAT and installation-token delegation while adding opt-in App JWT delegation and real-client integration coverage.
constraint: Stop and grill the user if a meaningful implementation question remains unresolved.
worktree: /Users/tyler/src/go-github-server/.worktrees/issue-4-app-jwt
branch: codex/issue-4-app-jwt
base: origin/main at f5f2149
skill_use: session-worklog source=pagerguild/core-tools -> capture commands, decisions, proof, and closeout state for this non-trivial repository change.
skill_use: proof-of-work source=pagerguild/core-tools -> publish runtime API-path evidence on the PR and follow exact-head CI through auto-merge.
skill_use: ship source=pagerguild/core-tools -> commit, push, and open the requested pull request without an extra confirmation stop.
skill_use: caveman-commit source=pagerguild/core-tools -> keep the staged-change commit message concise and conventional.

## Activity

- Refreshed `origin/main` and created the task worktree before repository changes.
- Read issue #4 with `gh issue view 4`; acceptance requires opt-in App JWT delegation, untouched credentials, unchanged PAT and installation-token paths, endpoint authorization remaining in the service, and integration coverage through a real `go-github` client and `httptest.Server`.
- Confirmed no `AGENTS.md`, `CLAUDE.md`, or `CONTEXT.md` is present in the repository root.
- Read the public README authentication contract and module/toolchain declaration.
- Added optional `AppJWTAuthenticator` delegation, JWT-shape classification, public documentation, and focused integration coverage.
- Ran the repository's configured modernization command. Its Go 1.26 default rewrote pre-existing pointer helpers and a few standard-library idioms in touched Go files; retained those formatter changes so the worktree matches the configured pre-commit result.

## Decisions

decision: Add a separate optional `AppJWTAuthenticator` interface so existing `Authenticator` implementations remain source-compatible.
decision: Classify App JWT candidates only by three non-empty dot-separated segments; do not decode, parse, or validate JWT contents in the server library.
decision: Preserve PAT and installation-token prefix dispatch before JWT-shape dispatch, and return 401 when an authenticator has not opted into App JWTs.

## Proof

- `go test ./...` -> pass (`githubserver` and `cmd/gen-server`).
- `go test -race ./...` -> pass.
- `go run ./cmd/gen-server -check` -> pass.
- `go tool goimports -w server.go server_test.go` -> clean.
- `go tool modernize -fix ./...` -> completed; applied configured modernization.
- `golangci-lint run ./...` -> `0 issues.`
- `git diff --check` -> pass.
- Targeted verbose acceptance run passed: existing PAT/installation delegation; App JWT through real `go-github` `Apps.CreateInstallationToken`; delegate rejection of invalid/expired/future-issued/overlong/wrong-algorithm/unknown-App candidates; installation-token issuance rejection without App identity; legacy authenticator opt-in behavior.

## Closeout

status: implementation complete and local proof passing; publication and exact-head proof in progress.
files_changed: `server.go`, `server_test.go`, `README.md`, `llms.txt`, and this worklog.
branch_state: `codex/issue-4-app-jwt` tracks `origin/main`; branch was two commits behind current main when publication began and will be rebased before exact-head proof.
remaining_debt: none identified within issue #4 scope.
