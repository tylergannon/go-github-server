# GitHub server generator worklog

goal: Generate a complete server-side inverse of the `github` package in `google/go-github`, preserving service boundaries, using route metadata and AST analysis, with typed handlers, entity reuse, authentication delegation, and end-to-end tests.
constraint: Only the upstream `github` package is in scope; exclude scrape, script, example, otel, test, and tools.
constraint: Keep the design basic and avoid speculative layers.
worktree: `/Users/tyler/src/go-github-server` on unborn `main`; an isolated worktree cannot be created because the repository has no commits.
source: `/Users/tyler/src/go-github` at `c0e23235476f55cb3f358ef3f18d886e6f8f872e` (`github.com/google/go-github/v89`).
skill_use: claude-codex-consensus source=pagerguild/core-tools -> requested independent design and implementation review.
skill_use: session-worklog source=pagerguild/core-tools -> preserve decisions, sources, review results, and proof.
initial_state: `go.mod`, `go.sum`, and `lefthook.yml` initialized; pre-existing `HANDOFF.md` describes an unimplemented prior design discussion.
doc_lookup: `../go-github/github/gen-accessors.go` and `../go-github/tools/metadata/metadata.go` -> upstream uses `go/ast`, `go/parser`, comment association, templates, and `go/format`, but not type checking.
doc_lookup: `../go-gen-jsonschema/internal/syntax/loader.go` -> its `go/packages` configuration demonstrates the `NeedTypes` and `NeedTypesInfo` layer required here; `dave/dst` is unnecessary because this generator emits new files rather than rewriting decorated source.
source_inventory: upstream has 204 production Go files, 43 routed services, and 1262 route annotations; 42 use the shared `service` alias while `MarketplaceService` is a distinct struct and is also exposed directly by `github.Client`; 33 client methods describe two routes; exceptional cases include projected bodies, raw and redirect responses, two uploads, one form endpoint, and duplicate method-path descriptions.
external_resource: https://docs.github.com/en/rest/authentication/authenticating-to-the-rest-api -> both `Bearer` and `token` authorization schemes can carry PATs or installation tokens; JWTs require `Bearer`.
external_resource: https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/about-authentication-to-github -> documented prefixes are `ghp_` and `github_pat_` for PATs and `ghs_` for installation access tokens, including the new stateless format.
review: Claude design review at `ephemeral/reviews/2026071201-claude-design.md`; session `792dc385-ff7d-4563-bd31-28f2d76aee6b`.
decision: Use standard `go/ast` plus type-aware `go/packages`; do not use `dave/dst` because no source-preserving rewrite is needed.
decision: Generate one interface and one unimplemented embedding per routed upstream service (43 including the structurally distinct MarketplaceService), plus a Services aggregate and grouped route registrations.
decision: Keep upstream method signatures for the initial inverse transport contract so application code receives familiar `go-github` entities and can set status and headers through `*github.Response`; generated adapters own HTTP decoding and encoding.
decision: Classify authorization credentials only by documented GitHub prefixes after parsing either supported header scheme; pass the full untouched token to the authenticator delegate. Unknown token families and Basic auth are rejected.
decision: Group identical method-path registrations before touching `ServeMux`; `/hub` is one form-discriminated handler rather than two conflicting registrations.
review: Claude implementation review at `ephemeral/reviews/2026071202-claude-implementation.md`; session `5ad05983-b45d-45ca-ac5b-20dc380fd5fc`.
correction: Exact route grouping originally selected only the first generated method outside `/hub`, making primary operations such as `GetContents` unreachable. The review reproduced this with a real client and a 501 response.
decision: Shared HTTP operations now order candidates by upstream `Accept` metadata and direct transport ownership, then fall through only on `ErrNotImplemented`. Content-negotiated siblings are independently selectable; convenience and cross-service aliases can be implemented through either service surface.
correction: Binary uploads originally failed to bind `*os.File`; handlers now spool the inbound body to a temporary file for file-based methods and pass the raw body for `io.Reader` methods. Upload metadata continues through query options and HTTP headers.
correction: Redirect-or-stream download methods now distinguish streamed bodies from redirect strings and set `Location` for redirects.
decision: Duplicate annotations that cannot independently reconstruct the annotated path from their client convenience signature are reported as `generated-alias` and omitted from route candidates when a canonical shared operation exists.
decision: Path values projected from entity fields such as `Repository.ID` and `EncryptedSecret.Name` are rehydrated into the typed argument before application dispatch.
proof: Real-client tests now cover webhook JSON/query/path/status round trips, `/hub` form collision dispatch, PAT and installation-token delegation, content convenience aliases, release metadata-vs-download Accept dispatch, binary upload bytes, cross-service alias fallback, entity-field path projection, and JSON/diff/patch/SHA Accept negotiation.
review: Claude post-fix review at `ephemeral/reviews/2026071203-claude-fix-review.md`; session `fa358d37-680b-4fe6-b1da-c17e0db4ae90`.
correction: Redirect classification based only on direct `bareDoUntilFound` calls missed five methods returning `*url.URL`; response kind is now derived from the public result signature, and string redirect results are also accepted.
correction: Client control parameters (`maxRedirects`, archive format selectors) and composite compare refs cannot be mapped by raw scalar position. Generated overrides now use the actual literal artifact format, materialize archive format/ref separately, and split `{basehead}` into base and head.
correction: Request bodies projected into one-field wire wrappers (for example `selected_repository_ids`) previously attempted to decode the wrapper object directly into a slice. AST analysis now records composite-literal/map field names and emits field-level JSON bindings.
decision: Single-operation request bodies stream directly to their adapter; buffering/replay is limited to shared-operation fall-through groups, so binary uploads are not first loaded entirely into memory.
decision: Default response status is explicitly 200 with a body and 204 without one; endpoint-specific success statuses come from the implementation's returned `*github.Response`.
decision: Removed unused jsonschema generator tools and their transitive `dave/dst` dependency; retained only goimports and modernize, which Lefthook uses.
proof: Real-client tests additionally cover artifact/run-log/migration/archive redirects, composite compare refs, projected single-field JSON bodies, and all URL-result signatures. Full `go generate`, generator check, test, race, vet, and golangci-lint gates pass; lint reports zero issues.
review: Claude final review at `ephemeral/reviews/2026071204-claude-final.md`; session `e60af0a7-7c21-41ad-84b6-c6d930fa74e3`.
correction: Response kind is now result-signature-first. Incidental `parseBoolResponse` and `bytes.Buffer` use can no longer misclassify JSON entity/slice results; raw and bool classifications are generator-tested against their declared result types.
correction: Redirect delegation through `UserMigrationArchiveURL` is recognized, and formatted URL query values such as `includes_parents`, `since`, and `s` are AST-bound to scalar method parameters.
correction: Path bindings now prefer the actual `fmt.Sprintf` construction and its parameter dependencies over raw scalar order, fixing authenticated-user route selectors and other multi-route signatures.
decision: Helper-delegated parameters not otherwise proven by direct AST data flow use a generated automatic binding adapter. Such routes are classified with an `automatic parameter binding` reason rather than advertised as clean; real-client proof covers helper-delegated JSON bodies.
proof: Final audit confirms 43 service interfaces and all 1262 annotations classified (`1039 generated-clean`, `217 generated-with-override`, `6 generated-alias`). Generation/check, unit and real-client tests, race detector, vet, golangci-lint, module verification, coverage invariants, and `git diff --check` all pass.
final_state: Repository remains on unborn `main`; all implementation files are untracked because the user did not request a commit. Existing work was not staged or committed.
