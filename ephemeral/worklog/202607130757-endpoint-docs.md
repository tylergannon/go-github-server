# Endpoint documentation generation

- goal: Copy upstream go-github method documentation onto generated service interface methods and add explicit HTTP method/path documentation.
- worktree: `/Users/tyler/src/go-github-server/.worktrees/endpoint-docs`
- branch: `codex/endpoint-docs`
- skill_use: session-worklog source=pagerguild/core-tools -> record generator decisions and proof.
- decision: Use the method AST comment group already loaded from github.com/google/go-github/v89; exclude internal `meta:operation` directives and emit generated HTTP route lines separately.
- decision: Document effective adapter routes rather than repeating known annotation errors for `DownloadArtifact` and `GetLatestCodespaceExport`.
- source_discovery: All 1,229 generated interface methods have upstream prose. Three intentionally have no GitHub docs URL because upstream identifies them as undocumented endpoints; their warnings and routes are preserved.
- licensing: Added `THIRD_PARTY_NOTICES.md` with the upstream BSD 3-Clause notice because generated source now copies upstream documentation comments.
- proof: `go run ./cmd/gen-server -check`, `go test ./...`, `go test -race ./...`, `go vet ./...`, `golangci-lint run ./...`, `go mod verify`, and `git diff --check` passed.
- final_state: 1,262 HTTP route documentation lines emitted across 1,229 generated interface methods; internal `meta:operation` directives do not appear in generated output.
